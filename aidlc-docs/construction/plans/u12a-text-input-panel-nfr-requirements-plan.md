# NFR Requirements Plan: U12-A テキスト入力パネル

## ユニットコンテキスト

- **ユニット**: U12-A テキスト入力パネル
- **スコープ**: テキストエリア貼り付け、ファイル読み込み（.txt/.md）、バリデーション、ドラフト永続化（draft-{id}.json）、シーン分割委譲、タブ表示制御。
- **スコープ外**: シーン分割処理（U12-B）、素材推薦（U12-D）、JSON生成（U12-E）。

## NFR Requirements チェックリスト

- [x] U12-A Functional Design成果物を読み込む。
- [x] U12-AのNFR判断ポイントを特定する。
- [x] ユーザー回答を収集する。
- [x] 回答の曖昧さ・矛盾・不足を分析する（曖昧さなし）。
- [x] `nfr-requirements.md` を生成する。
- [x] `tech-stack-decisions.md` を生成する。
- [x] NFR Requirementsの完全性を確認する。

## 確認質問

すべての `[Answer]:` へ選択肢の文字を記入してください。

### 質問1
テキストエリアの操作レスポンス（入力・貼り付け）に目標を設けますか？

A) 入力・貼り付けは即時反映（ブラウザ標準動作）、ドラフト保存は500msデバウンスで非同期（推奨）

B) ドラフト保存を同期にし、保存完了を確認してから次のアクションを許可する

C) 目標は設けず、React状態管理の標準挙動に任せる

X) その他（記入）

[Answer]: a

### 質問2
ドラフト保存（draft-{id}.json書き込み）の信頼性はどう扱いますか？

A) 書き込み失敗はコンソールログのみ。メモリ上のテキストは保持し、次のデバウンス後に再試行する（推奨）

B) 書き込み失敗時にトースト通知でユーザーへ警告する

C) 書き込み失敗時は保存できない旨をインラインメッセージで表示する

X) その他（記入）

[Answer]: a

### 質問3
ファイル読み込みで受け付けるファイルサイズに上限を設けますか？

A) 上限なし（大きなファイルはそのまま読み込む）

B) 上限を設ける（例: 1MB超は「ファイルが大きすぎます」エラー）（推奨）

C) 上限は設けず、読み込み時間が長い場合はインジケーターを表示する

X) その他（記入）

[Answer]: b

### 質問4
U12-Aのテストでは何を必須にしますか？

A) バリデーションロジック（空テキスト・Workspace未選択）とドラフト保存ロジックの単体テストを必須にする（推奨）

B) Reactコンポーネントのレンダリングテスト（Testing Library）も必須にする

C) ファイル読み込みのElectron dialogモックテストも必須にする

X) その他（記入）

[Answer]: a
