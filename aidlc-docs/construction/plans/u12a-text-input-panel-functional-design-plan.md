# Functional Design Plan: U12-A テキスト入力パネル

## ユニットコンテキスト

- **ユニット**: U12-A テキスト入力パネル
- **優先度**: Must
- **目的**: 草案テキストをアプリに渡す入力ゲートウェイを追加する。
- **対象ストーリー**: US-1（テキストエリアへの草案貼り付け）、US-2（テキストファイルの読み込み）

## Functional Design チェックリスト

- [x] ユニット定義・ストーリーマップを読み込む。
- [x] U12-AのビジネスロジックとUIステート境界を特定する。
- [x] ユーザー回答を収集する。
- [x] 回答の曖昧さ・矛盾・不足を分析する（曖昧さなし）。
- [x] `business-logic-model.md` を生成する。
- [x] `business-rules.md` を生成する。
- [x] `domain-entities.md` を生成する。
- [x] `frontend-components.md` を生成する。
- [x] Functional Designの完全性を確認する。

## 確認質問

すべての `[Answer]:` へ選択肢の文字を記入してください。

### 質問1
「テキスト入力」タブはいつ表示しますか？

A) 常に表示する（Workspace未選択でも表示し、入力はできるがシーン分割はWorkspace選択後に可能）（推奨）

B) Workspace選択後にのみ表示する

C) 既存の「台本ドラフト」タブと同じ条件で表示する

X) その他（記入）

[Answer]: a

### 質問2
ファイル読み込み時に既存のテキストエリア内容はどうなりますか？

A) 確認ダイアログを表示し、承認後に上書きする（推奨）

B) 確認なしで上書きする

C) 既存テキストの末尾に追記する

X) その他（記入）

[Answer]: a

### 質問3
「シーンに分割する」ボタンを押した後、UIはどうなりますか？

A) テキスト入力タブのまま処理中インジケーターを表示し、完了後にシーン調整タブへ自動切り替えする（推奨）

B) テキスト入力タブのまま、シーン一覧を同一パネル内にインラインで表示する

C) 処理中は何も変えず、完了通知をトーストで表示する

X) その他（記入）

[Answer]: a

### 質問4
草案テキスト（テキストエリアの内容）はドラフトとして永続化しますか？

A) Workspace選択済みの場合、テキストも `draft-{id}.json` に含めて保存する（アプリ再起動後に復元）（推奨）

B) テキストはメモリのみ（シーン分割後にドラフトとして保存する）

C) テキストは保存しない

X) その他（記入）

[Answer]: a

### 質問5
受け付けるファイル形式と拒否時の挙動はどうしますか？

A) `.txt` / `.md` のみ受け付け、それ以外は「.txt または .md ファイルを選択してください」とエラーダイアログを表示する（推奨）

B) すべてのファイルを受け付け、テキストとして読み込む

C) `.txt` のみ受け付け、`.md` は拒否する

X) その他（記入）

[Answer]: a
